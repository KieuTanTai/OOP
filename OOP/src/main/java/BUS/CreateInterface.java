package BUS;

public interface CreateInterface {
     // interface for implements in all file on BUS's folder and it use for add some role for validate ...

     public abstract boolean checkInputId (String id);
     // public abstract boolean checkInputIndex (int index);
}
